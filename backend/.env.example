# Operator private key (hex with 0x prefix or base64)
# This account must be registered as an operator in the LP Registry contract
OPERATOR_PRIVATE_KEY=0x...

# Optional: Override poll interval (ms)
# POLL_INTERVAL=30000
